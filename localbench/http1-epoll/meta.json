{
  "display_name": "zix-localbench-http1-epoll",
  "language": "Zig",
  "type": "engine",
  "engine": "zix.Http1 EPOLL Dispatch Model",
  "description": "HTTP/1.1 on the zix.Http1 raw engine, .EPOLL dispatch: per-worker SO_REUSEPORT epoll loops, comptime Router, staged send sink. Dual listener: cleartext 8080, TLS 1.3 on 8081. Database routes run on engine-worker lanes, each worker pumping its own pipelined postgrez connections and waiting on its own reply with named prepared statements. No response cache and no startup pre-serialization anywhere: json serializes every field per request and compresses per request, static opens the file per request. The crud row cache is the 200 ms cache-aside the crud profile itself defines.",
  "repo": "https://codeberg.org/prothegee/zix",
  "enabled": true,
  "tests": [
    "baseline",
    "pipelined",
    "limited-conn",
    "upload",
    "static",
    "static-tls",
    "json",
    "json-comp",
    "json-tls",
    "api-4",
    "api-16",
    "async-db",
    "crud"
  ],
  "maintainers": [
    "prothegee"
  ]
}

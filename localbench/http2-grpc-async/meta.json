{
  "display_name": "zix-localbench-http2-grpc-async",
  "language": "Zig",
  "type": "engine",
  "engine": "zix.Grpc ASYNC Dispatch Model",
  "description": "gRPC over h2c on the zix.Grpc engine built on zix.Http2, .ASYNC dispatch: one accept thread hands each connection to a task that multiplexes its streams. Replies use comptime-cached HPACK blocks. Implements GetSum (unary) and StreamSum (server-streaming). No response cache: the reply is a few bytes, so a lookup would cost more than the add.",
  "repo": "https://codeberg.org/prothegee/zix",
  "enabled": true,
  "tests": [
    "unary-grpc",
    "stream-grpc"
  ],
  "maintainers": [
    "prothegee"
  ]
}

name: x86_64-openbsd

on:
  pull_request:
    types:
      - closed
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    name: x86_64-openbsd
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: main

      # No GitHub-hosted OpenBSD runner exists, so the job boots a real
      # OpenBSD VM on top of this ubuntu host and runs the suite inside it.
      # No docker daemon in that VM, so postgrez and rediz test-integration
      # (container lifecycle) are skipped here.
      - name: Test in OpenBSD
        uses: vmactions/openbsd-vm@v1
        with:
          release: "7.9"
          usesh: true
          prepare: |
            mkdir -p /usr/local/bin
            ftp -o /tmp/zig.tar.xz https://ziglang.org/download/0.16.0/zig-x86_64-openbsd-0.16.0.tar.xz
            tar -C /usr/local -xf /tmp/zig.tar.xz

            ln -sf /usr/local/zig-x86_64-openbsd-0.16.0/zig /usr/local/bin/zig
          run: |
            cd $GITHUB_WORKSPACE
            zig version

            zig build test-all
            zig build postgrez-test-unit
            zig build rediz-test-unit
            zig build prometheuz-test-unit
